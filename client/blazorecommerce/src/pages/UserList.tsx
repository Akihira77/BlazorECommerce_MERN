import React from "react";
import Index from "../components/UserList/Index.tsx";

type Props = {};

const UserList = (props: Props) => {
  return (
    <div>
      <Index />
    </div>
  );
};

export default UserList;
